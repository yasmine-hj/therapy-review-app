  </br>
<% if @therapy_group %>
    <h1>  All reviews for  <%= @therapy_group.name %>!</h1>
<% else %>
    <h1> All reviews</h1>        
  </br>
<% end %>

<div class=container3>
<% @reviews.each do |r| %>
    <h2> <%= link_to "#{r.therapy_group.name}", therapy_group_path(r.therapy_group) %> </h2> 
    <h3> <%= r.therapy_group.location %>  - <%= r.therapy_group.duration %> mins </h3>
    <h3> <%= r.title %>  - <%= r.stars %>/5 stars   <%= render_stars(r.stars) %></h3>
    <p> <%= r.content %> </p>
    <p> <%= link_to "view review", review_path(r) %></p>
<% end %>

<h3> <%= link_to "Write a new review", new_review_path %></h3> 
</div>